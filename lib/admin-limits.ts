/**
 * 관리자 일괄 작업의 조각 크기.
 *
 * 서버 액션 파일('use server')은 함수만 내보낼 수 있어 상수를 여기 둔다.
 * 서버와 화면이 같은 값을 보게 해서 한쪽만 바뀌는 일을 막는다.
 */

/** 메일 일괄 반영 — 한 호출에 처리할 인원 (한 사람당 원격 호출 2번) */
export const EMAIL_BATCH = 5;
